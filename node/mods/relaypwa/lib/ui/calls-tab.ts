/** Phase 3 scope (videocall/stun integration) -- not started yet. */
export function renderCallsTab(): string {
  return `
    <div class="relaypwa-calls-tab">
      <p class="relaypwa-coming-soon">Calls are coming soon.</p>
    </div>
  `;
}
